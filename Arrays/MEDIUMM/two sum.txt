two sum problem

has 2 varieties
1. yes or no
2. index of elements adding upto target

only indexing extra

BRUTE FORCE
- check with all
for(i=0;i<n;i++){
    for(j=0;j<n;j++){
        if(i==j) continue;
        if(arr[i]+arr[j]==target)
        i,j return
    }
}


another version
for(i=0;i<n;i++){
    for(j=i;j<n;j++){
        if(i==j) continue;
        if(arr[i]+arr[j]==target)
        i,j return
    }
}

---------------------------------------------------------
better solution - HASHING
for(i=0;i<n;i++){
    n=nums.size();
    for (i=0;i<n;i++){
        num = nums[i];
        more= target-nums
        if(map.find(more)!=map.end()){
            return (map(more),i);
        }
        map[num]=i;
    }
    return (-1,-1);
}

----------------------------------------------------------
BEST SOLUTION

arr.sort();
left = arr[0];
right = arr[n];
move left and right to traverse



